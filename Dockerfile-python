FROM mdotcarter/devenv

RUN sudo apt-get update && sudo apt-get install -y \
    python3-pip

# install pipenv
RUN pip3 install pipenv

# dotfile overrides
RUN ln -sf ${DEVENV}/dotfiles-python/zshrc ${HOME}/.zshrc
RUN ln -sf ${DEVENV}/dotfiles-python/vimrc ${HOME}/.vimrc
